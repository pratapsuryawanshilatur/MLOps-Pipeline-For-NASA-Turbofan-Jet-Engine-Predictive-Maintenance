apiVersion: v1
kind: Service
metadata:
  name: turbofan-api-service
  namespace: default
  labels:
    app: turbofan-api
    component: ml-serving
spec:
  selector:
    app: turbofan-api
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  type: NodePort