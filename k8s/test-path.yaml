apiVersion: v1
kind: Pod
metadata:
  name: test-path-pod
spec:
  containers:
  - name: test
    image: busybox
    command: ['sh', '-c', 'echo "Testing path access..." && ls -la /test-mount && echo "Files found:" && find /test-mount -type f && sleep 3600']
    volumeMounts:
    - name: test-volume
      mountPath: /test-mount
  volumes:
  - name: test-volume
    hostPath:
      path: /run/desktop/mnt/host/c/Users/hp/Downloads/mlops-turbofan-project/models
      type: Directory